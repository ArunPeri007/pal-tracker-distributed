applications:
- name: tracker-registration
  path: ./applications/registration-server/build/libs/registration-server.jar
  routes:
  - route: registration-pal-${UNIQUE_IDENTIFIER}.${DOMAIN}
  memory: 1G
  instances: 1
  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
    REGISTRATION_SERVER_ENDPOINT: http://registration-pal-ar.apps.evans.pal.pivotal.io
  services:
  - tracker-registration-database
